# Ask Aunt Lurlene needs NO external packages — it uses only Python's standard library.
# This file is intentionally empty of requirements so the build cannot fail on pip.
#
# (Optional) To switch to PostgreSQL for permanent shared storage, add a Railway
# PostgreSQL database and then uncomment the next line:
# psycopg2-binary==2.9.9
